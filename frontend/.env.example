# Vite env vars (copy this file to .env.local)

# API: mặc định dùng /api — Vite proxy (vite.config.ts) chuyển tới http://localhost:8080.
# Nếu thấy "http proxy error" / ECONNREFUSED: khởi động backend trước khi chạy npm run dev.
#   ./mvnw spring-boot:run   (cần MySQL, Redis theo src/main/resources/application.yaml)
# Tùy chọn trỏ thẳng tới API (không qua proxy):
# VITE_API_BASE=http://localhost:8080

# Google Maps JavaScript API key
VITE_GOOGLE_MAPS_API_KEY=AIzaSyCGDOKhVQ7u229wD4cx5J-rw_-TGryR23o

# Optional: Map ID for cloud-based map styling / advanced markers
# VITE_GOOGLE_MAP_ID=
